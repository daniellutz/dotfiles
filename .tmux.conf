# Set fish as the default shell (use the full path)
set-option -g default-shell /usr/bin/fish

# Optional: Ensure new windows use fish explicitly as a login shell
# This helps source config.fish correctly in all scenarios
set-option -g default-command "/usr/bin/fish -l"   
